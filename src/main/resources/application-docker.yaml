spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/multi_persistence_backend_postgres_db

  jpa:
    show-sql: false

  data:
    mongodb:
      uri: mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongo:27017/multi_persistence_backend_mongo_db?authSource=admin&retryWrites=false